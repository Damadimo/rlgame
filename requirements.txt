# RL stack needs PyTorch. As of early 2026, many macOS + Python 3.13 setups still get
#   ERROR: No matching distribution found for torch
# because there is no wheel for that combo. Use Python 3.11 or 3.12 (see .python-version if you use pyenv):
#   pyenv install 3.12.8 && pyenv local 3.12.8
#   python -m pip install -U pip
#   pip install -r requirements.txt
#
# Optional: helps pip find CPU wheels on some platforms.
--extra-index-url https://download.pytorch.org/whl/cpu

gymnasium>=0.29.0
numpy>=1.24.0
pygame>=2.5.0
stable-baselines3>=2.0.0
torch>=2.0.0
pytest>=7.0.0
